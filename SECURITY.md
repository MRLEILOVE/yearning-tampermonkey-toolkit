# 安全策略

## 报告安全问题

如果 GitHub 仓库已经启用 Private vulnerability reporting，请优先通过仓库的 Security 页面私下报告。

如果只能使用公开 Issue，请只描述复现步骤和影响，不要提交以下内容：

- Yearning 真实地址或内网拓扑
- 账号、密码、Cookie、JWT、Token 或请求头
- 数据库连接信息、真实库名和表名
- 业务 SQL、查询结果、页面截图中的生产数据

在维护者提供私密沟通渠道前，请使用脱敏占位符替代所有环境信息。

## 使用者安全建议

- 将每个脚本的 `@match` 限制到准确的 Yearning 地址。
- 安装或升级脚本前审阅源码和依赖地址。
- 只在受信任的浏览器环境中运行脚本。
- 对 `@require` 的第三方资源使用固定版本；受限环境可替换为经过审核的内部镜像。
- Yearning 升级后重新验证脚本行为，尤其是查询、认证信息读取和 WebSocket 相关功能。
